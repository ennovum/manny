let config = {
    "api": {
        "baseUrl": "//localhost:8080/api",
        "resources": {
            "refinance": {
                "verdict": "/refinance/verdict"
            }
        }
    }
};

export default config;
