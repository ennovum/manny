let config = {
    "api": {
        "baseUrl": "//localhost:8080/api",
        "resources": {
            "moneypl": {
                "history": "/allegro/costimize"
            }
        }
    }
};

export default config;
