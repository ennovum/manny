let config = {
    "api": {
        "baseUrl": "//" + window.location.host + "/api",
        "resources": {
            "refinance": {
                "verdict": "/refinance/verdict"
            }
        }
    }
};

export default config;
