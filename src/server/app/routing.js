let routing = {
    "routes": {
        "/moneypl/history": {
            "handler": "moneyplHandler",
            "method": "history"
        },
        "/refinance/verdict": {
            "handler": "refinanceHandler",
            "method": "verdict"
        }
    }
};

export default routing;
